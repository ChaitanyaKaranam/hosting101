import React from "react"
// import { Link } from "gatsby"

const Header = (props) => (
  <header>
    <nav>
      <ul>
        <li>Hosting101</li>
      </ul>
    </nav>
  </header>
)

export default Header
